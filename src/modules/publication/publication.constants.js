/**
 * Constantes du module Publication.
 */

const PUBLICATION_STATUS = {
  ACTIVE:   'active',
  INACTIVE: 'inactive',
  PENDING:  'pending',
  REJECTED: 'rejected',
};

module.exports = { PUBLICATION_STATUS };
